# 数据使用说明

(English version: DATA_GUIDE.md)

## 目录结构

样例包解压后的目录结构与付费归档完全一致，因此针对样例写的代码，换成正式交付的
数据集可以原样运行：

```
predict-fun-data-samples/
  data/predict-fun/prices/BTCUSDT/BTCUSDT-feed<id>-predict-prices-<日期>.csv.gz
  data/predict-fun/orderbook/BTC-5M/BTC-5M-predict-orderbook-<日期>.jsonl.gz
  data/predict-fun/markets/predict-markets-<日期>.jsonl.gz
  data/predict-fun/klines/BTCUSDT/1m/BTCUSDT-feed<id>-1m-<日期>.csv.gz
```

目录层级本身带有含义，工具会按它解析：`data/predict-fun/<数据类型>/<系列>/`，
其中价格与 K 线的系列键是符号（`BTCUSDT`），盘口的系列键是 `<资产>-<周期>`
（`BTC-5M`、`ETH-15M`、`BNB-DAILY`）。markets 表是每天一个文件、覆盖全部资产与
周期，因此没有系列目录。完整购买的数据集结构相同，只是币种、周期、13 个 K 线
周期与天数更多。

## <符号>-feed<id>-predict-prices-<日期>.csv.gz —— 结算价 tick

| 字段 | 含义 |
|---|---|
| price_feed_id | 上游 feed id（1 = BTCUSDT，2 = ETHUSDT，3 = BNBUSDT）|
| symbol | feed 符号，如 BTCUSDT |
| provider | 我们订阅该 feed 所用的通道——见下方说明（2026-09-03 及更早的数据请忽略此列）|
| publish_time | 价格事件时间（unix **秒**）|
| server_ts | 上游消息时间戳（unix **秒**）|
| price | 价格（float64）|
| recv_ms | 采集端接收时间（unix **毫秒**）|

关于 `provider`：这一列记录的是**我们订阅该 feed 所用的通道**（`CHAINLINK`，或走
pyth topic 的 feed 记为 `PYTH`）。它是 feed 的属性，不是价格的属性，**也不是上游
逐条给出的来源标注**。

**2026-09-03 及更早的数据里，这一列不可靠，请忽略。**当时它记录的是「我们的市场
发现循环最后处理到的那个市场所声明的结算源」——而同一条价格流被多个周期的市场共用，
各自声明不同（5m/15m 声明 `CHAINLINK`，1 小时与 24 小时声明 `BINANCE`），于是这个
标签会随我们每 10 秒的轮询来回跳。它不能说明任何一条价格的来源。该问题已于 2026-09-05 在采集端修复，
2026-09-04 起的数据带的是修正后的标签。

**价格数据本身从未受影响。**我方归档的每一条 tick 都来自同一个上游 topic
（`chainlinkAssetPriceUpdate/<feed_id>`），而该 feed 不携带任何逐条来源信息——因此这份
数据里不存在需要校正的「混合来源」结构，不影响波动率或任何其他统计量，也没有理由按
这一列过滤或分组；若您已按它拆分过数据，请合并回去。独立验证：2026-08-25 样例日，我方持有的 555 个结算边界秒 tick 全部精确复现
场方自己发布的 `start_price`/`end_price`。

限制：我方 tick 主键是 `(price_feed_id, publish_time, price)`，不含 `provider`，
重复帧会被吸收、只保留先到的那个标签。

关于精度：与我们的 Polymarket 结算价流不同，Predict.fun 只发布 float64 价格，
上游没有全精度整数字符串，因此也无从归档。tick 主键为
`(price_feed_id, publish_time, price)`，用于吸收上游 WebSocket 对同一 publish
时间重复推送的帧。

## <资产>-<周期>-predict-orderbook-<日期>.jsonl.gz —— 盘口快照

系列键为 `<资产>-<周期>`，如 `BTC-5M`、`ETH-15M`、`BNB-DAILY`。

| 字段 | 含义 |
|---|---|
| market_id | 上游市场 id |
| category_slug | 市场 slug，后缀为 slot 起点（unix 秒）|
| update_ts_ms | 上游盘口更新时间（毫秒）|
| recv_ms | 采集端接收时间（毫秒）|
| payload | 上游快照对象，逐字原样存储 |

`payload` 内含 `bids` / `asks`（`[价格, 数量]` 数对），以及上游自带的 `version`、
`marketId`、`orderCount`、`lastOrderSettled`、`settlementsPending` 等字段，全部
按收到的样子保留。

注：这里**只有全量快照**。Predict.fun 的推送流不发布盘口增量，因此与我们的
Polymarket 数据集不同，这里没有 `price_change` 增量序列，也没有成交流水。
t 时刻盘口 = 该市场 `recv_ms <= t` 的最近一条快照。

限流（已公示）——它变过一次，跨越该日期时请注意：

- 截至 2026-08-24：每市场每秒最多保留 1 条快照
- **2026-08-25 起：完全不限流，上游推送的每一条快照都已归档**

此前的限流丢弃了上游快照中的绝大多数，因此 2026-08-25 起的日子盘口细节约为此前的
十倍。可自行对照：样例日 BTC-5M 有 679,335 条快照，而 2026-07-24 只有 68,398 条。

## predict-markets-<日期>.jsonl.gz —— 市场元数据与结算结果

每天一个文件，覆盖全部资产与周期。

| 字段 | 含义 |
|---|---|
| category_slug | 市场 id，后缀为 slot 起点（unix 秒）|
| asset | btc / eth / bnb |
| interval_label | `5m` / `15m` / `hourly` / `daily`——见下方说明 |
| market_id | 上游市场 id（与盘口文件关联）|
| price_feed_id / price_feed_symbol | 结算该市场的 feed |
| price_feed_provider | 该市场的结算源：5m 与 15m 为 `CHAINLINK`，daily 为 `BINANCE` |
| condition_id | 链上 condition id |
| start_sec / end_sec | 局的起止时间（unix 秒）|
| start_price | 起点价（strike）——Up 需严格高于它才算赢 |
| end_price | 结算价 |
| status | 我们**最后一次**读到该市场时上游给的状态——不是结算标志，见下方说明 |

周期标签：**2026-09-05 之前导出**的文件把 1 小时市场标成了 `daily`。那是我方 slug
解析的 bug（一条 `up-or-down` 正则同时吃掉了两类市场），自该日起已修正。24 小时市场的
slug 带 `-on-`（`bitcoin-up-or-down-on-september-5-2026`），1 小时市场则以小时结尾
（`bitcoin-up-or-down-september-5-2026-5am-et`）。若需在旧文件上还原真实周期，
`end_sec - start_sec` 永远是权威。

结算规则有三种结果：`end_price > start_price` → Up 赢；`end_price < start_price`
→ Down 赢；`end_price == start_price` → 该局为**平局退款（push）**，场方把两边都判为
赢、本金退回。平局在这里不算 Up 赢（与 Polymarket 相反），且 5 分钟局约每 100 局就有
1 局收在平局，用 `>=` 这种二元判据会把它们判错。两个值都来自上游市场对象，因此仅凭
本文件即可核对结果；价格文件则用于复盘中间过程。

**不要用 `status` 判断一个局是否已结算。**我们在读到 `end_price` 之后就不再重读该
市场，而在那一刻场方往往仍把它报为 `OPEN`——于是 `status` 就冻结在那个取值上。5m/15m 大多数确实是 `RESOLVED`（约 92%），但由币安结算的那两类几乎从不翻转——
**已结算的 24 小时市场 255 个里 0 个、小时市场 0.65%**——按 `status == 'RESOLVED'`
过滤会把它们几乎全部静默丢掉。可靠的判据是
`end_price` 是否存在：我们的全部历史里，没有任何一行是 `RESOLVED` 却缺 `end_price`。

## <符号>-feed<id>-<周期>-<日期>.csv.gz —— K 线

| 字段 | 含义 |
|---|---|
| ts_ms | 桶开始时间（毫秒）|
| open / high / low / close | 桶内 float64 价格 |
| ticks | 该桶聚合的 tick 条数 |

1s..1d 为按天文件；3d / 1w / 1mo 为全历史快照，命名带 `-thru-<日期>`，且只含已
完结的桶。K 线由上面的价格 tick 衍生而来——你随时可以自己重算。

## manifest.json —— 文件清单，每个文件的行数与 sha256 校验值

本仓库每个样例文件都与付费数据集中对应文件逐字节一致；sha256 与归档自带的校验
索引相同。
